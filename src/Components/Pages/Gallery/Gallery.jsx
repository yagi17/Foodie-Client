import { Helmet } from "react-helmet";

const Gallery = () => {
  return (
    <div>
      <Helmet>
        <title>Foodie | Gallery</title>
      </Helmet>
    </div>
  );
};

export default Gallery;
